name: Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        type: choice
        options:
          - dev
          - stage
        default: dev
      mode:
        description: 'Deployment mode'
        type: choice
        options:
          - deploy
          - what-if
        default: deploy
      deployContainerApps:
        description: 'Deploy Container Apps (requires ghcrToken)'
        type: boolean
        default: false
      ghcrToken:
        description: 'GitHub PAT for ghcr.io (required if deployContainerApps=true)'
        type: string
        default: ''
  push:
    branches: [main, develop]
    paths:
      - 'infra/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'infra/**'

# Prevent concurrent deployments to the same environment
concurrency:
  group: infra-${{ github.event.inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  validate:
    name: Validate Bicep
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Validate Bicep syntax
        run: az bicep build --file infra/main.bicep

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: ARM Validate (dev parameters)
        run: |
          az deployment sub validate \
            --location eastus2 \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.dev.json \
            --parameters servicePrincipalObjectId='00000000-0000-0000-0000-000000000000'

      - name: ARM Validate (stage parameters)
        run: |
          az deployment sub validate \
            --location eastus2 \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.stage.json \
            --parameters servicePrincipalObjectId='00000000-0000-0000-0000-000000000000'

  deploy:
    name: Deploy Infrastructure
    runs-on: ubuntu-latest
    needs: validate
    # Only deploy on push to main/develop or manual trigger (not PRs)
    if: github.event_name != 'pull_request'
    environment: ${{ github.event.inputs.environment || 'dev' }}

    permissions:
      id-token: write  # Required for OIDC
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get Service Principal Object ID
        id: sp
        run: |
          # Get the object ID of the service principal for RBAC assignments
          OBJECT_ID=$(az ad sp show --id ${{ secrets.AZURE_CLIENT_ID }} --query id -o tsv)
          echo "object_id=$OBJECT_ID" >> $GITHUB_OUTPUT

      - name: Deploy Infrastructure (What-If)
        if: github.event.inputs.mode == 'what-if'
        run: |
          ENV=${{ github.event.inputs.environment || 'dev' }}
          az deployment sub what-if \
            --location eastus2 \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.${ENV}.json \
            --parameters servicePrincipalObjectId='${{ steps.sp.outputs.object_id }}' \
            --parameters deployContainerApps=${{ github.event.inputs.deployContainerApps || 'false' }} \
            --parameters ghcrToken='${{ github.event.inputs.ghcrToken }}'

      - name: Deploy Infrastructure
        if: github.event.inputs.mode != 'what-if'
        run: |
          ENV=${{ github.event.inputs.environment || 'dev' }}
          az deployment sub create \
            --location eastus2 \
            --name "sessionsight-infra-$(date +%Y%m%d-%H%M%S)" \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.${ENV}.json \
            --parameters servicePrincipalObjectId='${{ steps.sp.outputs.object_id }}' \
            --parameters deployContainerApps=${{ github.event.inputs.deployContainerApps || 'false' }} \
            --parameters ghcrToken='${{ github.event.inputs.ghcrToken }}'

      - name: Provision Managed Identity SQL users
        if: github.event.inputs.mode != 'what-if'
        run: |
          ENV=${{ github.event.inputs.environment || 'dev' }}
          RG="rg-sessionsight-dev"
          DB_NAME=$( [ "$ENV" = "dev" ] && echo "sessionsight" || echo "sessionsight-${ENV}" )
          API_APP="sessionsight-${ENV}-api"

          # Skip if Container App doesn't exist yet
          if ! az containerapp show -g ${RG} -n ${API_APP} --query name -o tsv 2>/dev/null; then
            echo "Container App ${API_APP} not found, skipping MI user provisioning"
            exit 0
          fi

          # Install Go-based sqlcmd (supports ActiveDirectoryDefault auth via az login)
          curl -s https://packages.microsoft.com/keys/microsoft.asc | sudo tee /etc/apt/trusted.gpg.d/microsoft.asc
          sudo add-apt-repository "$(curl -s https://packages.microsoft.com/config/ubuntu/22.04/prod.list)"
          sudo apt-get update -y && sudo apt-get install -y sqlcmd

          # Create MI user for the Container App (drop first to resolve stale SID)
          sqlcmd -S "sessionsight-sql-dev.database.windows.net" -d "${DB_NAME}" \
            --authentication-method ActiveDirectoryDefault \
            -Q "IF EXISTS (SELECT 1 FROM sys.database_principals WHERE name = '${API_APP}')
                  DROP USER [${API_APP}];
                CREATE USER [${API_APP}] FROM EXTERNAL PROVIDER;
                ALTER ROLE db_owner ADD MEMBER [${API_APP}];"

          echo "MI user [${API_APP}] provisioned in database [${DB_NAME}]"

      - name: Show Deployment Outputs
        if: github.event.inputs.mode != 'what-if'
        run: |
          echo "## Deployment Outputs" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Get the latest sessionsight deployment (reverse sort by name which includes timestamp)
          DEPLOYMENT_NAME=$(az deployment sub list \
            --query "[?starts_with(name, 'sessionsight-infra-')].name | reverse(@) | [0]" \
            -o tsv)

          echo "Deployment: $DEPLOYMENT_NAME" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Show outputs as JSON (more reliable than table format)
          az deployment sub show \
            --name "$DEPLOYMENT_NAME" \
            --query properties.outputs \
            -o json >> $GITHUB_STEP_SUMMARY

  preview:
    name: Preview Changes (PR)
    runs-on: ubuntu-latest
    needs: validate
    # Only run what-if on PRs
    if: github.event_name == 'pull_request'
    environment: dev

    permissions:
      id-token: write
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get Service Principal Object ID
        id: sp
        run: |
          OBJECT_ID=$(az ad sp show --id ${{ secrets.AZURE_CLIENT_ID }} --query id -o tsv)
          echo "object_id=$OBJECT_ID" >> $GITHUB_OUTPUT

      - name: Infrastructure What-If (Dev)
        run: |
          az deployment sub what-if \
            --location eastus2 \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.dev.json \
            --parameters servicePrincipalObjectId='${{ steps.sp.outputs.object_id }}' \
            --no-pretty-print 2>&1 | tail -200 > /tmp/whatif-dev.txt || true

      - name: Infrastructure What-If (Stage)
        run: |
          az deployment sub what-if \
            --location eastus2 \
            --template-file infra/main.bicep \
            --parameters infra/main.parameters.stage.json \
            --parameters servicePrincipalObjectId='${{ steps.sp.outputs.object_id }}' \
            --no-pretty-print 2>&1 | tail -200 > /tmp/whatif-stage.txt || true

      - name: Comment PR with What-If Results
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const devOutput = fs.readFileSync('/tmp/whatif-dev.txt', 'utf8').trim();
            const stageOutput = fs.readFileSync('/tmp/whatif-stage.txt', 'utf8').trim();

            const body = `### Infrastructure Changes Preview

            #### Dev Environment
            \`\`\`
            ${devOutput}
            \`\`\`

            #### Stage Environment
            \`\`\`
            ${stageOutput}
            \`\`\`

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
